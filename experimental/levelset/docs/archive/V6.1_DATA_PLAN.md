# v6.1 数据生成与训练计划

## 📋 执行计划

### 第 1 步: 生成 v6.1 训练数据

**命令**:
```bash
cd /home/scnu/Gitee/EFD3D/levelset
python3 generate_levelset_data.py \
    --num-interface 50000 \
    --num-ink 50000 \
    --num-polar 50000 \
    --output data/levelset_training_data_v6.1_large.pt
```

**预期输出**:
- 总样本数: 150,000 (50K interface + 50K ink + 50K polar)
- 文件大小: ~40-50MB
- 数据格式: 6D 输入 + 归一化 ψ 目标值

### 第 2 步: 验证数据文件

**命令**:
```bash
python3 -c "
import torch
data = torch.load('data/levelset_training_data_v6.1_large.pt')
print(f'Total samples: {len(data[\"interface_points\"]) + len(data[\"ink_points\"]) + len(data[\"polar_points\"])')
print(f'interface: {len(data[\"interface_points\"])}')
print(f'ink: {len(data[\"ink_points\"])}')
print(f'polar: {len(data[\"polar_points\"])}')
"
```

### 第 3 步: 启动训练

**命令**:
```bash
cd /home/scnu/Gitee/EFD3D/levelset
python3 train_levelset_3d.py --config config/v6.1_fixed.json
```

**预期时长**: 
- v6.1 配置: 50000 epochs
- A100 GPU: ~4-5 小时
- RTX 4090: ~5-6 小时

### 第 4 步: 监控训练进度

**关键指标**:
```bash
# 实时监控日志
tail -f outputs_levelset_*/training.log

# 检查体积守恒
grep "VolCon" outputs_levelset_*/training.log | tail -20

# 检查 NaN 恢复
grep "NaN" outputs_levelset*/training.log | tail -10

# 检查 Loss 变化
grep "Epoch.*Loss:" outputs_levelset*/training.log | tail -30
```

## 📊 预期改进效果

| 指标 | v6.0 (现有) | v6.1 (预期) | 改进幅度 |
|------|-----------|-------------|---------|
| 体积守恒误差 | +35.42% | < ±1% | **>95%** ↓ |
| 训练完成率 | 4-5% | 100% | **100%** ↑ |
| NaN 崩溃 | 频繁 | 0 次 | **100%** ↓ |
| Loss 震荡 | 多次尖峰 | 减少 >50% | **>50%** ↓ |

## ⚠️ 故障排查

如果训练中断:
```bash
# 1. 检查显存
nvidia-smi

# 2. 检查日志最后几行
tail -50 outputs_levelset_*/training.log

# 3. 从检查点恢复
python3 train_levelset_3d.py \
    --config config/v6.1_fixed.json \
    --resume outputs_levelset_*/checkpoint_epoch_XXXX.pt
```

## 📁 文件说明

| 文件 | 用途 |
|------|------|
| `generate_levelset_data.py` | 数据生成脚本 |
| `train_levelset_3d.py` | 训练脚本（已修改） |
| `pinn_levelset_3d.py` | 模型（已修改） |
| `config/v6.1_fixed.json` | v6.1 配置文件 |
| `data/levelset_training_data_v6.1_large.pt` | 训练数据（待生成） |

## ✅ 验证清单

- [ ] v6.1 数据文件已生成
- [ ] 数据样本数正确 (150,000)
- [ ] 数据格式验证通过
- [ ] v6.1 配置文件已就位
- [ ] 训练已开始
- [ ] VolCon 指标 < 0.01
- [ ] 无 NaN 崩溃
- [ ] 训练稳定进行

---

生成时间: 2026-02-03
